{
    "private": true,
    "scripts": {        
        "dev": "vite",
        "build": "vite build",
        "prod": "npm run build",
        "production": "npm run build"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}